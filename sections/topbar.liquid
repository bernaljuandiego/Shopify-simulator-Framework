<section class="topbar">
  <div class="topbar__marquee">
    <div class="topbar__content">
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
    </div>
    <div class="topbar__content" aria-hidden="true">
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
      <span class="topbar__text">{{ settings.top-bar.settings.marquee_text }}</span>
      <div class="topbar__icon">{% render 'star' %}</div>
    </div>
  </div>
</section>
